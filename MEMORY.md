# MEMORY

## Erros registrados

- 2026-04-24: Ao criar o repositório privado, usei `gh repo create ... --remote=origin` mesmo já existindo um remoto `origin` local. O repo foi criado, mas a etapa de adicionar remoto falhou. Correção aplicada: validei o repo e executei `git push -u origin main`. Próxima vez: verificar `git remote -v` e, se `origin` já existir, criar o repo sem tentar recriar o remoto ou apenas fazer push.
- 2026-04-24: Encerrei um `./gradlew bootRun` matando o PID pela porta, o que gerou falha Gradle com exit 143. Correção aplicada: passei a iniciar `bootRun` com TTY e encerrar com Ctrl+C. Próxima vez: usar TTY para processos longos que precisam de encerramento limpo.
- 2026-04-24: Ao conectar WebSocket no Angular, deixei `sockjs-client` ser carregado sem garantir `window.global`, causando `ReferenceError: global is not defined` no browser. Correção aplicada: adicionei polyfill em `index.html` e `src/polyfills.ts`, além de import dinâmico do SockJS. Próxima vez: validar dependências browser/node em runtime real, não só em teste.
- 2026-04-24: Iniciei download em background dentro do fluxo de criação antes da entidade estar definitivamente persistida, causando download completo com status preso em `queued`. Correção aplicada: usei `saveAndFlush` antes de iniciar job e normalizei downloads completos na inicialização. Próxima vez: separar persistência confirmada e execução assíncrona.
- 2026-04-24: Comitei `src/polyfills.ts` usado pelo Angular production build sem incluir o arquivo em `tsconfig.app.json`, fazendo o GitHub Actions falhar no job frontend. Correção aplicada: adicionei `src/polyfills.ts` em `tsconfig.app.json` e validei com `npm run build`. Próxima vez: antes de push que altera Angular config, rodar também build de produção, não apenas teste.
- 2026-04-24: Configurei o Electron Builder para gerar `.deb` sem `homepage`, `author.email` e `linux.maintainer`, fazendo o job `package (ubuntu-latest)` falhar no GitHub Actions. Correção aplicada: adicionei metadados reais no `electron/package.json`. Próxima vez: antes de habilitar targets Linux `.deb`/`rpm`, validar os metadados exigidos pelo Electron Builder.
- 2026-04-24: Deixei o build de produção do Angular depender de fetch externo para inline de Google Fonts, o que quebrou o job `package (ubuntu-latest)` quando o runner não conseguiu acessar a URL. Correção aplicada: mantive otimização de produção e desativei apenas `optimization.fonts.inline`. Próxima vez: builds de CI e empacotamento desktop não devem depender de recursos externos em tempo de build.
- 2026-04-24: Coloquei nome, e-mail e URL pessoal em metadados versionados do Electron e do projeto. Correção aplicada: removi os dados pessoais do código e passei a usar metadados genéricos não pessoais. Próxima vez: nunca gravar dados pessoais em arquivos versionados ou metadados de empacotamento.
